# This is a basic workflow to help you get started with Actions

name: CI

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  prepare:
    name: Prepare for Xamarin
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: setup-xamarin
      uses: maxim-lobanov/setup-xamarin@v1
      with:
        mono-version: 6.12 # specify version in '<major>.<minor>' format
        xamarin-ios-version: 14 # specify version in '<major>' format
        xamarin-mac-version: latest # specify 'latest' keyword to pick up the latest available version
        xamarin-android-version: 11.2 # specify full version; it is not recomended option because your pipeline can be broken suddenly in future
        xcode-version: 12 # set the latest available Xcode 11
    - name: Prepare Xamarin
      run: |
        nuget restore ${{ github.workspace }}/Covid19Radar.sln
    - name: Cache Xamarin
      uses: actions/cache@v2
      env:
        cache-name: cache-xamarin
      with:
        path: ~/.nuget/packages
        key: {{ $GITHUB_SHA }}
  build_ios:
    name: Build-iOS
    runs-on: macos-latest
    needs: prepare
    strategy:
      matrix:
        Configuration: [Debug, Debug_Mock, Release]
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Cache Xamarin
      uses: actions/cache@v2
      env:
        cache-name: cache-xamarin
      with:
        path: ~/.nuget/packages
        key: {{ $GITHUB_SHA }}
    - name: iOS.${{ matrix.Configuration }}
      run: |
        msbuild ${{ github.workspace }}/Covid19Radar/Covid19Radar.iOS/Covid19Radar.iOS.csproj /t:Rebuild /p:Configuration=${{ matrix.Configuration }} /p:Platform=iPhoneSimulator
  build_android:
    name: Build-Android
    runs-on: macos-latest
    needs: prepare
    strategy:
      matrix:
        Configuration: [Debug, Debug_Mock, Release]
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Cache Xamarin
      uses: actions/cache@v2
      env:
        cache-name: cache-xamarin
      with:
        path: ~/.nuget/packages
        key: {{ $GITHUB_SHA }}
    - name: Android.${{ matrix.Configuration }}
      run: |
        msbuild ${{ github.workspace }}/Covid19Radar/Covid19Radar.Android/Covid19Radar.Android.csproj /t:Rebuild /p:Configuration=${{ matrix.Configuration }}
  test:
    name: Test
    runs-on: macos-latest
    needs: prepare
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Cache Xamarin
      uses: actions/cache@v2
      env:
        cache-name: cache-xamarin
      with:
        path: ~/.nuget/packages
        key: {{ $GITHUB_SHA }}
    - name: Tests
      run: |
        dotnet test ${{ github.workspace }}/Covid19Radar/Tests/Covid19Radar.UnitTests/
      env:
        DOTNET_CLI_TELEMETRY_OPTOUT: true